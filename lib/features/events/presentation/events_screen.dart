import 'package:flutter/material.dart';

/// Platzhalter — Kalender & Kontext-Cockpit folgt in M2.
/// AppBar/Scaffold stellt `AppShell` bereit.
class EventsScreen extends StatelessWidget {
  const EventsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Kalender / Termine — folgt in M2'));
  }
}
