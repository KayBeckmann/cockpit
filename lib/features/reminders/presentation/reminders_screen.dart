import 'package:flutter/material.dart';

/// Platzhalter — Erinnerungen & n8n-Integration folgt in M3.
/// AppBar/Scaffold stellt `AppShell` bereit.
class RemindersScreen extends StatelessWidget {
  const RemindersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Erinnerungen — folgt in M3'));
  }
}
