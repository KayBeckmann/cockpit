import 'package:flutter/material.dart';

/// Platzhalter — vollständige Aufgabenverwaltung folgt in M1.
/// AppBar/Scaffold stellt `AppShell` bereit.
class TasksScreen extends StatelessWidget {
  const TasksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Aufgaben & GTD — folgt in M1'));
  }
}
