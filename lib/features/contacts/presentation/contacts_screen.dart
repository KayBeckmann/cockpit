import 'package:flutter/material.dart';

/// Platzhalter — Kontaktpflege folgt in M3.
/// AppBar/Scaffold stellt `AppShell` bereit.
class ContactsScreen extends StatelessWidget {
  const ContactsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Kontakte & Beziehungspflege — folgt in M3'));
  }
}
