import 'package:flutter/material.dart';

/// Platzhalter — Obsidian-Integration folgt in M6 (ADR-0002: Obsidian bleibt das Wiki).
/// AppBar/Scaffold stellt `AppShell` bereit.
class WikiScreen extends StatelessWidget {
  const WikiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Obsidian-Integration — folgt in M6'));
  }
}
