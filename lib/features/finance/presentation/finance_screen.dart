import 'package:flutter/material.dart';

/// Platzhalter — Finanzen & Budgets folgt in M4 (Kontext hart getrennt, ADR-0006).
/// AppBar/Scaffold stellt `AppShell` bereit.
class FinanceScreen extends StatelessWidget {
  const FinanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Finanzen & Budgets — folgt in M4'));
  }
}
