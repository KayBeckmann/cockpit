import 'package:flutter/material.dart';

/// Platzhalter — Kanban-Basis folgt in M1, Zeiterfassung & Erweiterungen in M5.
/// AppBar/Scaffold stellt `AppShell` bereit.
class ProjectsScreen extends StatelessWidget {
  const ProjectsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Projekte (Kanban) — folgt in M1 / M5'));
  }
}
